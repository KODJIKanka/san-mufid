import { firestore } from "firebase-admin";
import FieldValue = firestore.FieldValue;

export type TortoiseUpdateInput<T> = {
  [K in keyof T]?: T[K] | FieldValue
}

export type TortoiseDataInput<T> = {
  [K in keyof T]: T[K] | FieldValue
}